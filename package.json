{
  "devDependencies": {
    "@types/node": "^18.6.4",
    "gts": "^3.1.0",
    "typescript": "^4.7.4"
  },
  "scripts": {
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run lint"
  }
}
